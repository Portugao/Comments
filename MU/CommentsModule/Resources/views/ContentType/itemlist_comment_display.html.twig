{# Purpose of this template: Display comments within an external context #}
{% for comment in items %}
    <h3>{{ comment|mucommentsmodule_formattedTitle }}</h3>
    <p><a href="{{ path('mucommentsmodule_comment_display', {'id': comment.getId()}) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
